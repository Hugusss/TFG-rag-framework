"""Document loading seam: raw dataset formats become canonical Documents.

Loaders are the only components that know dataset file formats. The rest
of the pipeline sees :class:`rag_framework.models.Document` and nothing
else.
"""
