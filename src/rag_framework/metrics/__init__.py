"""Metrics seam: benchmark and report output belongs here.

Every result file the project writes carries the reproducibility
manifest produced in this package, so a number can always be traced
back to the code, corpus and machine that produced it.
"""
